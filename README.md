# Mick's Notes

A small Jekyll blog, published with GitHub Pages. Essays on building software with
AI agents.

- Posts live in `_posts/` as Markdown (`YYYY-MM-DD-title.md`).
- The site renders at https://huamick.github.io/blog/
- To add a post: drop a new Markdown file in `_posts/` with front matter and push.
